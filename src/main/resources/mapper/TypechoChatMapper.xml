<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.RuleApi.dao.TypechoChatDao">

    <resultMap id="BaseResultMap" type="com.RuleApi.entity.TypechoChat" >
        <result column="id" property="id" />
        <result column="chatid" property="chatid" />
        <result column="uid" property="uid" />
        <result column="toid" property="toid" />
        <result column="created" property="created" />
        <result column="lastTime" property="lastTime" />
        <result column="type" property="type" />
    </resultMap>

    <sql id="Base_Column_List">
        `id`,
        `chatid`,
        `uid`,
        `toid`,
        `created`,
        `lastTime`,
        `type`
    </sql>

    <!-- 插入数据 -->
    <insert id="insert" parameterType="com.RuleApi.entity.TypechoChat"  keyProperty="id" useGeneratedKeys="true">
        INSERT INTO typecho_chat
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test ='null != id'>
                `id`,
            </if>
            <if test ='null != chatid'>
                `chatid`,
            </if>
            <if test ='null != uid'>
                `uid`,
            </if>
            <if test ='null != toid'>
                `toid`,
            </if>
            <if test ='null != created'>
                `created`,
            </if>
            <if test ='null != lastTime'>
                `lastTime`,
            </if>
            <if test ='null != type'>
                `type`
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test ='null != id'>
                #{id},
            </if>
            <if test ='null != chatid'>
                #{chatid},
            </if>
            <if test ='null != uid'>
                #{uid},
            </if>
            <if test ='null != toid'>
                #{toid},
            </if>
            <if test ='null != created'>
                #{created},
            </if>
            <if test ='null != lastTime'>
                #{lastTime},
            </if>
            <if test ='null != type'>
                #{type}
            </if>
        </trim>
    </insert>

    <!-- 批量插入数据 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO typecho_chat ( <include refid="Base_Column_List" /> ) VALUES
        <foreach collection="list" item="curr" index="index" separator=",">
            (
                #{curr.id},
                #{curr.chatid},
                #{curr.uid},
                #{curr.toid},
                #{curr.created},
                #{curr.lastTime},
                #{curr.type}
            )
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="com.RuleApi.entity.TypechoChat">
        UPDATE typecho_chat
        <set>
            <if test ='null != chatid'>`chatid` = #{chatid},</if>
            <if test ='null != uid'>`uid` = #{uid},</if>
            <if test ='null != toid'>`toid` = #{toid},</if>
            <if test ='null != created'>`created` = #{created},</if>
            <if test ='null != lastTime'>`lastTime` = #{lastTime},</if>
            <if test ='null != type'>`type` = #{type}</if>
        </set>
        WHERE `id` = #{id}
    </update>

    <!-- 删除 -->
    <delete id="delete">
        DELETE FROM typecho_chat
        WHERE `id` = #{key}
    </delete>

    <!-- 批量删除 -->
    <delete id="batchDelete" parameterType = "java.util.List">
        DELETE FROM typecho_chat WHERE id IN
        <foreach collection="list"  item="item" open="(" separator="," close=")"  >
            #{item}
        </foreach>
    </delete>

    <!-- 主键查询 -->
    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM typecho_chat
        WHERE `id` = #{key}
    </select>

    <!-- 条件查询 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM typecho_chat
        <where>
            <if test ='null != id'>
                and `id` = #{id}
            </if>
            <if test ='null != chatid'>
                and `chatid` = #{chatid}
            </if>
            <if test ='null != uid'>
                and `uid` = #{uid}
            </if>
            <if test ='null != toid'>
                and `toid` = #{toid}
            </if>
            <if test ='null != created'>
                and `created` = #{created}
            </if>
            <if test ='null != lastTime'>
                and `lastTime` = #{lastTime}
            </if>
            <if test ='null != type'>
                and `type` = #{type}
            </if>
        </where>
    </select>

    <!-- 分页条件查询 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM typecho_chat
        <where>
            <if test ='null != typechoChat.id'>
                and `id` = #{typechoChat.id}
            </if>
            <if test ='null != typechoChat.chatid'>
                and `chatid` = #{typechoChat.chatid}
            </if>
            <if test ='null != typechoChat.uid'>
                and `uid` = #{typechoChat.uid}
            </if>
            <if test ='null != typechoChat.toid'>
                and `toid` = #{typechoChat.toid}
            </if>
            <if test ='null != typechoChat.created'>
                and `created` = #{typechoChat.created}
            </if>
            <if test ='null != typechoChat.lastTime'>
                and `lastTime` = #{typechoChat.lastTime}
            </if>
            <if test ='null != typechoChat.type'>
                and `type` = #{typechoChat.type}
            </if>
        </where>
        limit #{page,jdbcType=INTEGER}, #{pageSize,jdbcType=INTEGER}
    </select>

    <!-- 总量查询 -->
    <select id="total" resultType="java.lang.Integer">
        SELECT count(*) FROM typecho_chat
        <where>
            <if test ='null != id'>
                and `id` = #{id}
            </if>
            <if test ='null != chatid'>
                and `chatid` = #{chatid}
            </if>
            <if test ='null != uid'>
                and `uid` = #{uid}
            </if>
            <if test ='null != toid'>
                and `toid` = #{toid}
            </if>
            <if test ='null != created'>
                and `created` = #{created}
            </if>
            <if test ='null != lastTime'>
                and `lastTime` = #{lastTime}
            </if>
            <if test ='null != type'>
                and `type` = #{type}
            </if>
        </where>
    </select>
</mapper>